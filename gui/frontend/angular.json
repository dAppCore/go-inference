{
  "$schema": "./node_modules/@angular/cli/lib/config/schema.json",
  "version": 1,
  "newProjectRoot": "",
  "projects": {
    "lem-desktop": {
      "projectType": "application",
      "root": "",
      "sourceRoot": "src",
      "prefix": "lem",
      "architect": {
        "build": {
          "builder": "@angular/build:application",
          "options": {
            "outputPath": "dist",
            "index": "src/index.html",
            "browser": "src/main.ts",
            "tsConfig": "tsconfig.app.json",
            "styles": ["src/styles.css"],
            "allowedCommonJsDependencies": ["lit"]
          },
          "configurations": {
            "production": { "optimization": true, "outputHashing": "none" },
            "development": { "optimization": false, "sourceMap": true }
          },
          "defaultConfiguration": "production"
        },
        "serve": {
          "builder": "@angular/build:dev-server",
          "configurations": {
            "production": { "buildTarget": "lem-desktop:build:production" },
            "development": { "buildTarget": "lem-desktop:build:development" }
          },
          "defaultConfiguration": "development"
        }
      }
    }
  }
}
